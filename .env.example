# Copy this file to ".env" and add your key to enable real Claude-powered features.
# Without a key, MindSpace runs in DEMO MODE (locally simulated AI responses).

ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: change the port (default 3000)
# PORT=3000
